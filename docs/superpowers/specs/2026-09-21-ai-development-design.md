# AI 经济发展调整（已确认方案）

用户确认采用原生评分优先方案：减弱国家身份导致的决策差异，适度增加资源投资、工业化、建造与生产方式调整意愿。

基线为本机 Victoria 3 1.13.11、Tech & Res 1.6、Firefall 0.1.1；按照约定完整加载栈调查，不以本机课程 playset 或旧日志推断当前存档表现。主要源码证据：原版 `00_default_strategy.txt`、`00_ai.txt`，Tech & Res / Firefall 的 `ztr_admin_strategies.txt`、`ztr_default_strategy.txt`。尚无当前存档运行证据。

## 实施范围

1. 在 Core 注入资源与工业战略的权重和建筑组权重。资源基础30、工业40，识字率系数均为 `clamp(0.75 + literacy × 0.5, 0.75, 1.25)`；强势实业家给工业额外10%。移除这两项权重中的国家、地区、年代及历史人物/日志条件，保留上游法律资格、商品和补贴定义。
2. 资源建筑组1.5→1.65；工业战略制造业1.25→1.5，资源组1.25→1.375。
3. 默认建造目标保留原版经济输入、人口、传统主义与外交支出算法；投资池收入扣除7500→2500；删除按国家等级加25/50/75及德日中专属乘数。投资池积压支出系数0.75→0.85；债务停建/恢复不变。
4. PM考虑切换概率0.1→0.2；高产出候选惯性0.95→1.0；等量/较低产出候选0.75→0.8。后者也覆盖部分自动化和降级，不是“自动化必选”。保留原生利润、亏损、就业与短缺处罚。
5. 本栈的上游AI调用缺失 `kai_has_high_supply`。提供同名、同参数的市场价格判断：沿用 `PRODUCTION_BUILDING_OUTPUT_HIGH_PRICE_THRESHOLD`，不增加 Kuromi AI 依赖，不复制战略全文。

## 实现与验收清单

- 只新增两个战略子表注入、一个建造子表注入、一个兼容trigger、四项define。
- 不调用 Flavor、Auto-Apply 或 Pruning 内部接口；无额外扫描、持久变量、强制战略/PM或资源产量补偿。
- 校验最终子表合并、保留宣称/殖民定义、策略上下界、建造阈值与财政输入、本地化及脚本结构。
- 独立提交 Core；未启动游戏，不提交运行日志或本机路径。

## 兼容与运行验收

无需迁移；已选战略要等原生重评，建筑与PM要等原生评估。新游戏和旧档分别观察同等财政下的强弱国建造、资源缺口、失业、PM变化、债务与停建；特别检查 Auto-Apply 是否后写。保留真实资源与技术差异，不能承诺实际增长率相同。没有实机结果前，不把静态场景当成发展改善证明。

## 本次静态结果

通过原版→Tech & Res→Firefall→Core子表合并检查、14组战略权重及360组建造场景；宣称/殖民子表保持原样。约定完整栈后加载模块中未发现同名经济注入/define或供应trigger覆盖。19个脚本结构、元数据、13个双语键及 `git diff --check` 通过。供应trigger与现有上游scope先例一致。未运行游戏。
